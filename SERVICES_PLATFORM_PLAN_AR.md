# خطة نظام الخدمات

الفرع الحالي: `feat/services-platform`

## مبدأ التنفيذ
- كل شيء مقسم إلى تطبيقات Django واضحة، ويكون الخادم هو صاحب القرار.
- العميل يرى كتالوج الخدمات وحالة طلبه فقط؛ بيانات اعتماد المزودين ومسارات الربط الداخلية ليست واجهة عامة.
- كل طلب خدمة يدخل طابورًا دائمًا، والتنفيذ يكون حسب `available_at` ثم `id`، أي بالتسلسل وليس عشوائيًا.
- قبل أول إرسال خارجي، يحجز النظام مبلغ العميل بقيد محاسبي. لا يُسمح بتنفيذ الخدمة عند عدم كفاية الرصيد.
- نجاح المزود يسوي الحجز إلى إيراد خدمات، والفشل يعيد المبلغ للمحفظة.
- حالة `Pending` محفوظة وتولد مهمة فحص حالة، وعند وجود مسار حالة لدى الربطية لا يُعاد إرسال عملية التسديد نفسها.
- فحص رصيد المزود يتم إداريًا مباشرة عبر بروتوكول المزود، ولا ينشئ معاملة عميل ولا قيدًا محاسبيًا.

## قائمة المهام

| المرحلة | الحالة | العمل |
|---|---|---|
| 1 | مكتملة | تطبيق الخدمات ونموذج الفئات الرئيسية وفئات الخدمات والخدمات والحقول الديناميكية |
| 2 | مكتملة | مزودون مستقلون وربطيات API متعددة مع بيانات اعتماد مشفرة |
| 3 | مكتملة | جدول توزيع خدمة ← ربطية مع أولوية ومساحة شروط |
| 4 | مكتملة | جداول متخصصة للفئات/الفئات الرصيدية والباقات ومنتجات الألعاب والبرامج |
| 5 | مكتملة | طلب الخدمة + الحجز المحاسبي + إعادة الرصيد عند الفشل |
| 6 | مكتملة | قائمة مهام متسلسلة + إعادة محاولات + متابعة Pending |
| 7 | مكتملة | واجهة إدارة عربية مخصصة + Django Admin مساعد |
| 8 | مكتملة | تعريفات ربط جاهزة لخدمات مستند API + فحص رصيد المزود من لوحة الربطيات |
| 9 | لاحقًا | ربط خدمة الشراء بالواجهات الأمامية الحالية وعرض النماذج الديناميكية |
| 10 | لاحقًا | عامل إنتاج دائم (systemd/PM2) وقياسزمن التنفيذ والمراقبة والتنبيهات |

## إعداد صنعاء كاش
الوثيقة المصدر تحدد `Api url`، و`Userid` و`DomainName` و`Username` و`Password`، وتستخدم MD5 لكلمة المرور ثم MD5 للقيمة المركبة من hash كلمة المرور و`transid` و`Username` و`mobile`. كما تذكر `resultCode=0` للنجاح و`-2` للعمليات المعلّقة. لذلك تحفظ هذه القيم في الربطية ويولد النظام الـtoken على الخادم وقت التنفيذ. راجع `api 1 (59).pdf` عند إدخال كل مسار فعلي.

## أمثلة المسارات من الوثيقة
- يمن موبايل: `yem?action=query` للاستعلام و`yem?action=bill` للتسديد، و`queryoffer`/`billoffer` للباقات.
- سبأفون: `sabaphone` للشحن، `sabaoffer` للباقات، إضافة إلى `sbay` و`sabaunits` للخدمات الأخرى.
- واي: `why?action=bill` للرصيد والباقات.
- يمن فورجي: `yem4g?action=bill` و`yem4g?action=query`.
- الألعاب والبطاقات: `gameswcards` مع `type` و`uniqcode` وحقول اللاعب المطلوبة.
- حالة العملية: `info?action=status` باستخدام نفس `transid`، وحساب رصيد المزود عبر `info?action=balance`.
