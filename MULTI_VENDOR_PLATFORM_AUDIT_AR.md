# تقرير تدقيق منصة الـ Multi-Vendor — فرع `feat/erp-control-current-shell-2026-09`

تاريخ التدقيق: 2026-09-17  
الفرع المفحوص: `feat/erp-control-current-shell-2026-09`  
آخر commit أثناء التدقيق: `812d60ecf035d7f91ddb1bacc061a8d677515b2d`

## 1. الهدف

الهدف هو تحويل لوحة التحكم الجديدة إلى طبقة إدارة متكاملة لمنصة متعددة المتاجر، بحيث تكون العلاقات بين **المتجر ← المنتجات ← الطلبات ← الدفع ← الشحن ← المحادثة ← المالية** قابلة للرؤية والإدارة من داخل نفس الـ ERP shell، مع الحفاظ على الـ Header والـ Sidebar الحاليين وعدم الرجوع للواجهات القديمة.

تم استخدام مشروع [Instashop-multi-vendors-ecommerce](https://github.com/MostafaHamedAbdelmasoud/Instashop-multi-vendors-ecommerce) كمرجع وظيفي للمقارنة، وليس كنموذج تقني يجب نسخه؛ فالمرجع مبني على Laravel/Vue/AdminLTE بينما مشروعنا Django.

## 2. الخلاصة التنفيذية

البنية الخلفية في الفرع الحالي أقوى من الواجهة الحالية في عدة أجزاء: نماذج البيانات تحتوي على تصنيفات متعددة للمنتج، متغيرات، صور، الطلبات المقسمة حسب المتجر، الدفع، الشحن، سجل حالات الطلب، حجوزات المخزون، ومحادثات مرتبطة بالطلب.

الفجوة الأساسية هي أن لوحة `/admin/dashboard/control/` لا تكشف كل هذه العلاقات بعد بشكل متكامل، وبعض تفاعلات JavaScript تعتمد على حالة URL الحالية. لذلك تظهر مشكلة أن زر **إضافة منتج** يمكن أن يفتح محرر التعديل عند بقاء `?edit=...` في العنوان، كما أن صفحة المنتجات الحالية لا تمثل دورة إدارة المنتج كاملة (الصور/المتغيرات/تفاصيل التصنيف) ولا توجد بعد صفحة تفصيل موحدة تربط المنتج والمتجر والطلب والمحادثة.

## 3. المشاكل المثبتة

| الأولوية | المجال | الملاحظة المثبتة | المعالجة المطلوبة |
|---|---|---|---|
| P0 | إضافة منتج | زر الإضافة يفتح نفس modal الموجود داخل الـ DOM، بينما حالة `edit_product` تأتي من `?edit=`؛ عند البقاء على شاشة تعديل يمكن أن تُستخدم نفس الحالة بدل إنشاء نموذج جديد | فصل create عن edit على مستوى JavaScript وURL والسياق الخادمي، مع فتح نموذج جديد نظيف صراحة |
| P0 | تعديل المنتج | `ProductForm` يدعم `categories`، لكن إدارة المنتج الحالية ليست كافية لإدارة جميع العلاقات المرتبطة به | إظهار جميع الحقول القابلة للتعديل، التصنيفات M2M، الصور الإضافية، المتغيرات، وحالة النشر/الترند والمخزون |
| P0 | تفاصيل الطلب | قائمة الطلبات الحالية مبنية على pagination وإحصاءات، لكنها لا تقدم مسار تفصيل إداري موحد | إنشاء صفحة تفصيل الطلب داخل الـ shell تشمل العميل، العناصر، المتاجر، الدفع، الشحن، سجل الحالة، المخزون، والإجراءات |
| P0 | محادثة الطلب | توجد نماذج `OrderChat` و`OrderChatMessage` لكن لا يوجد ربط ظاهر كامل داخل شاشة الطلب | إضافة تبويب/قسم محادثة داخل تفاصيل الطلب مع عرض الرسائل والمرفقات وحالة المحادثة |
| P1 | تفاصيل المنتج | يوجد تقرير مبيعات فقط؛ التقرير لا يساوي صفحة تفصيل كاملة | إنشاء Product Detail يربط المتجر والتصنيفات والمتغيرات والصور والمخزون والمبيعات والتقييمات والطلبات المرتبطة |
| P1 | تفاصيل المتجر | يوجد تقرير متجر، لكن صفحة تفصيل موحدة للمتجر غير مكتملة | إنشاء Store Detail يشمل المالك، بيانات المتجر، الصور، الحالة، العمولة، المنتجات، الطلبات، الشحن، المالية، والمحادثات |
| P1 | تعديل المتجر | `VendorProfileForm` يحتوي الحقول الأساسية، لكن تجربة التعديل يجب أن تضمن عرض كل قيمة حالية ومعاينة الصور والإعدادات | بناء محرر متجر كامل مع owner، الاسم، slug، الوصف، الشعار، الغلاف، الهاتف، العنوان، الحالة، العمولة، والإعدادات JSON بصورة آمنة |
| P1 | المتغيرات | `ProductVariant` موجود في قاعدة البيانات، لكن تعديل المنتج الحالي لا يدير المتغيرات كجزء من editor موحد | إضافة إدارة CRUD للمتغيرات داخل المنتج مع SKU/لون/مقاس/سعر/مخزون/حالة |
| P1 | الصور | `ProductImage` موجود ككيان مستقل، بينما `Product.images` أيضاً JSON | توحيد تجربة الإدارة دون كسر التوافق: الصورة الرئيسية + معرض الصور + النص البديل + الترتيب + الصورة الأساسية |
| P1 | الطلبات متعددة المتاجر | `VendorOrder` موجود ويقسم الطلب إلى أجزاء حسب التاجر | إظهار مستوى الطلب العام ومستوى كل متجر داخل نفس صفحة التفاصيل، مع إجماليات العمولة والصافي |
| P1 | الدفع والشحن | `Payment` و`Shipment` موجودان في backend، لكن شاشة تفاصيل عملية الإدارة تحتاج ربطهما بالطلب | إضافة أقسام payment/shipment مع المعرّف وحالة العملية والتتبع والمبالغ والمزود |
| P1 | سجل الطلب | `OrderStatusHistory` و`InventoryReservation` موجودان | إظهار timeline للحالة والحجوزات بدل الاكتفاء بالحالة الحالية |
| P1 | البحث والتصفية | توجد فلاتر أساسية لبعض القوائم | توحيد البحث والتصفية والفرز والتنقل بين التفاصيل مع الحفاظ على سياق القائمة |
| P1 | الروابط المتقاطعة | العناصر تعرض اسم المتجر أو المنتج نصياً في مواضع متعددة | تحويلها إلى روابط مباشرة داخل نفس shell: متجر ↔ منتج ↔ طلب ↔ عميل ↔ محادثة |
| P2 | الكتالوج | توجد Category وCatalogOption وPriceGroup وCity في backend | استكمال واجهات الإدارة المرتبطة بها بحيث تصبح جزءاً فعلياً من دورة إدارة الكتالوج والتسعير والشحن |
| P2 | المالية | يوجد ledger وpayout وcurrency/shipping data | جعلها قابلة للتصفح من المتجر والطلب مع drill-down واضح |
| P2 | الاتصالات | توجد Conversation/Message وOrderChat/OrderChatMessage وواجهات REST لها | إضافة واجهة إدارية مرئية وإدارة للقراءة/الإرسال/الإغلاق حسب الصلاحيات |
| P2 | الإشعارات | نموذج Notification وواجهة API موجودان | إضافة مركز إشعارات إداري مرتبط بالطلب/المنتج/المتجر |
| P2 | العملاء والصلاحيات | النظام يحتوي أدوار وصلاحيات متعددة في أجزاء أخرى | ربط صلاحيات الـ Control shell بكل الإجراءات الحساسة، لا بمجرد `is_staff` فقط |
| P2 | الاختبارات | توجد اختبارات في المشروع وCI، لكن يجب إضافة اختبارات Regression للمشاكل الحالية | اختبارات create/edit product، categories، store edit، order detail، chat rendering، والتنقل داخل shell |
| P2 | الإعلاميات والتحليلات | توجد إحصاءات أساسية | توسيع التحليلات إلى مبيعات المتاجر والمنتجات والطلبات والمدفوعات والشحن والمخزون |
| P2 | التكامل | المرجع Instashop يوضح أهمية REST API وmedia management والاختبارات | مراجعة تغطية API لكل الموارد التي أصبحت قابلة للإدارة من الويب |

## 4. ما هو موجود فعلياً في نموذج البيانات

### المنتجات

`Product` يحتوي على المتجر، التصنيفات المتعددة، SKU، الاسم، slug، الوصف، العلامة، الخامة، تعليمات الشحن والمرتجع، الأسعار، العملة، المخزون، الألوان، المقاسات، الوسوم، التفاصيل، الصورة الرئيسية، الصور، التقييم، حالة النشر، الترند، وعدد المبيعات.

كما توجد كيانات `ProductImage` و`ProductVariant` لإدارة المعرض والمتغيرات.

### المتاجر

`VendorProfile` يحتوي على المالك، اسم المتجر، slug، الوصف، الشعار، الغلاف، الهاتف، العنوان، الحالة، نسبة العمولة، والإعدادات.

### الطلبات

`Order` يحتوي العميل، رقم الطلب، الحالة، الإجماليات، العملة، عنوان الشحن، وسيلة الدفع، حالة الدفع والبيانات الإضافية.

ويتفرع إلى `OrderItem` ثم `VendorOrder` و`VendorOrderItem` لكل متجر مشارك، مع `Payment` و`Shipment` و`InventoryReservation` و`OrderStatusHistory`.

### المحادثة

يوجد مستويان: `Conversation/Message` للمحادثات العامة المرتبطة بالمتجر/الطلب، و`OrderChat/OrderChatMessage` لمحادثة الطلب لكل متجر؛ وهذا يسمح ببناء شاشة طلب متكاملة دون اختراع نموذج جديد.

## 5. المقارنة الوظيفية مع Instashop

بحسب README الحالي للمشروع المرجعي، فهو يعلن عن: multi-vendor architecture، لوحة خاصة بالتاجر لإدارة المتجر والمخزون، RBAC بأدوار متعددة، دعم عربي/إنجليزي RTL، real-time chat بين البائع والعميل، تكامل الدفع والشحن، REST API، إدارة الوسائط للمنتجات، واختبارات PHPUnit، مع AdminLTE/Vue في طبقة الإدارة.

مصدر المقارنة: [Instashop README](https://github.com/MostafaHamedAbdelmasoud/Instashop-multi-vendors-ecommerce/blob/master/README.md)

### البنود التي يجب أن نغطيها في مشروعنا

1. **Vendor lifecycle:** طلب متجر → مراجعة → قبول/رفض → متجر فعال → تعليق.
2. **Store management:** بيانات المتجر، المالك، الصور، الإعدادات، العمولة، الحالة.
3. **Catalog:** منتجات، تصنيفات، متغيرات، صور، أسعار، مخزون، حالات النشر.
4. **Order lifecycle:** الطلب العام، تقسيمه حسب المتاجر، حالات التنفيذ، الدفع، الشحن، التسليم، الإرجاع/الاسترداد.
5. **Communication:** محادثة العميل مع المتجر/الطلب وعرض المرفقات وحالة الإغلاق.
6. **RBAC:** صلاحيات دقيقة على القراءة والإنشاء والتعديل والحذف والإجراءات الحساسة.
7. **Media:** رفع وإدارة الصور بدلاً من الاكتفاء بروابط أو قيم نصية.
8. **API parity:** الموارد الإدارية المهمة يجب أن يكون لها API متسق حيث يلزم للتطبيقات.
9. **Bilingual/RTL:** الاستمرار في العربية RTL مع بنية قابلة للتعريب.
10. **Testing:** اختبارات Regression لمسارات العمل الأساسية وليس فقط النماذج المنفصلة.

## 6. خطة التنفيذ المعتمدة بعد التقرير

### المرحلة A — إصلاح مسار المنتج

- فصل create عن edit.
- إزالة اعتماد فتح الـ modal على حالة `edit` السابقة.
- تمرير التصنيفات المختارة عند التعديل بشكل صحيح.
- إدارة الصورة الرئيسية ومعرض الصور.
- إدارة المتغيرات من داخل المنتج.
- إضافة Product Detail شامل.

### المرحلة B — المتجر

- محرر Store كامل.
- معاينة الشعار والغلاف.
- صفحة Store Detail.
- ربط المنتجات والطلبات والشحن والمالية والمحادثات من صفحة المتجر.

### المرحلة C — الطلبات

- إصلاح القائمة والفلاتر.
- صفحة Order Detail.
- عرض العناصر الدقيقة مع صورة/متجر/SKU/لون/مقاس/كمية/سعر.
- عرض Vendor Orders.
- عرض Payment وShipment.
- Timeline للحالة والحجوزات.
- Chat للطلب داخل نفس الشاشة.

### المرحلة D — تكامل الواجهات

- Applications
- Categories
- Variants
- Orders
- Payments
- Finance
- Notifications
- Support/Conversations
- Customers/RBAC

مع الالتزام بأن المحتوى كله يبقى داخل الـ ERP shell الحالي.

## 7. معايير قبول المرحلة

لن نعتبر لوحة الإدارة متكاملة إلا عندما ينجح السيناريو التالي من البداية للنهاية:

**إنشاء متجر → تعديل كل بياناته → فتح تفاصيله → إنشاء منتج → تحديد أكثر من تصنيف → رفع الصورة → إنشاء المتغيرات → تعديل المنتج وإعادة فتحه مع كل البيانات → فتح المنتج من المتجر → فتح طلب مرتبط → رؤية كل عناصره الدقيقة → رؤية تقسيم الطلب حسب المتاجر → رؤية الدفع والشحن والحالة والتاريخ → فتح محادثة الطلب ورؤية رسائلها → الرجوع من الطلب إلى المنتج والمتجر دون مغادرة shell.**

## 8. ملاحظات تقنية مهمة

- لا نحتاج إلى تغيير نماذج البيانات لمجرد دعم واجهة الإدارة الحالية؛ معظم الكيانات الضرورية موجودة بالفعل.
- أي تعديل على `Product` يجب أن يحافظ على الـ M2M categories ولا يقتصر على `form.save()` فقط إذا كانت بيانات العلاقات تُرسل يدوياً.
- أي تعديل على `VendorProfile` يجب أن يحافظ على الملفات الحالية عند عدم رفع ملف بديل.
- صفحات التفاصيل يجب أن تكون GET-only للعرض، بينما إجراءات الحالة/الإرسال/التعديل تكون POST/CSRF ومحمية بالصلاحيات.
- يجب الحفاظ على الأداء باستخدام `select_related/prefetch_related` في التفاصيل والقوائم لتجنب N+1.

## 9. حالة التقرير

هذا commit هو **تقرير تدقيق قبل التنفيذ**.  
الخطوة التالية هي تنفيذ الإصلاحات المذكورة في هذا التقرير على نفس الفرع، ثم إضافة اختبارات Regression والتحقق من التدفقات الأساسية.
