# تقرير تسليم منصة التخفيض الصح (Multi-Vendor)

بناءً على طلبك، قمنا بتحليل التطبيق المرفق وإعادة هيكلته وتوسعته ليصبح منصة سوق إلكتروني متكاملة (Multi-Vendor Marketplace) تدعم التجار المتعددين، والعملاء، والإدارة العليا. 

## المكونات الأساسية المضافة

### 1. خادم Django & REST API
تم بناء طبقة خادم كاملة باستخدام **Django** و **Django REST Framework** في مجلد `backend` داخل المشروع. 
- **النماذج المضافة:** المستخدم (بأدوار العميل، التاجر، المدير)، المتاجر، المنتجات، الطلبات وبنودها (لتوزيع العمولات)، التصميم الديناميكي، المحفظة، الإشعارات، المحادثات، الكوبونات، والإحالات.
- **عزل البيانات:** تم تطبيق صلاحيات صارمة تمنع التاجر من رؤية طلبات ومنتجات تاجر آخر، بينما يملك المدير رؤية شاملة.
- **لوحة تحكم المدير:** تم تخصيص لوحة `Django Admin` قوية وجاهزة للعمل لإدارة التجار، العمولات، الموافقة على المنتجات، ومراجعة طلبات شحن المحافظ.

### 2. تطبيق التاجر المستقل (Vendor APK)
بدلاً من بناء تطبيق منفصل تمامًا من الصفر، قمنا بتعديل بنية **Expo Router** و `app.config.ts` لتدعم إخراج نسختين من نفس المشروع:
- **تطبيق العميل:** يعمل كالمعتاد، ويبدأ من شاشة المتجر الرئيسية `/(tabs)`.
- **تطبيق التاجر:** عند بنائه، يتم توجيه المستخدم فورًا إلى شاشة `vendor/login`، وتتغير حزمة التطبيق (Package Name) واسمه ليكون تطبيقًا مستقلاً يمكن تثبيته بجانب تطبيق العميل.
- **ميزات التاجر:** لوحة معلومات سريعة للمبيعات والعمولة، إدارة المنتجات والمخزون، التحكم بالتصميم والألوان، ومحفظة التاجر لسحب الرصيد.

### 3. التصميم الديناميكي والمحفظة
- **التصميم:** يستطيع التاجر تحديد اللون الرئيسي لمتجره واختيار إظهار أو إخفاء البانر الرئيسي. يتم جلب هذه الإعدادات ديناميكيًا من الخادم.
- **المحفظة:** تم إضافة نظام محفظة متكامل يسجل حركات الشحن، الدفع، والعمولات. يمكن للتاجر طلب سحب رصيد، ويمكن للعميل شحن رصيده.

## كيفية التشغيل وإخراج التطبيقات (APK)

لإخراج ملفات الـ APK، يمكنك استخدام خدمة **EAS Build** من Expo. قمنا بتجهيز ملف `eas.json` و `package.json` لتسهيل العملية:

1. **لإخراج تطبيق العميل (APK):**
   ```bash
   pnpm run apk:customer
   ```
2. **لإخراج تطبيق التاجر (APK):**
   ```bash
   pnpm run apk:vendor
   ```

هذه الأوامر ستوجه خدمة EAS لبناء تطبيقين منفصلين (بمعرفين مختلفين `com.app.truediscountfashion` و `com.app.truediscountfashion.vendor`) من نفس الكود.

## كيفية تشغيل الخادم محليًا

للعمل على الخادم وتجربته محليًا، اتبع الخطوات التالية من داخل مجلد `backend`:

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python manage.py migrate
python manage.py createsuperuser  # لإنشاء حساب المدير
python manage.py runserver 0.0.0.0:8000
```
بعدها يمكنك الوصول إلى لوحة الإدارة عبر `http://localhost:8000/admin`.

## الخطوات القادمة للإنتاج
- **الخادم:** يجب نقل قاعدة البيانات من SQLite إلى PostgreSQL، وضبط إعدادات `CORS` و `ALLOWED_HOSTS` في ملف `settings.py`.
- **الصور:** ربط `MEDIA_ROOT` بخدمة تخزين سحابية مثل AWS S3 لضمان عدم ضياع صور المنتجات والمتاجر.
- **الدفع:** دمج بوابات دفع محلية لشحن المحافظ بشكل آلي بدلاً من الطلبات اليدوية.

تم تضمين المشروع بالكامل في الملف المرفق. يمكنك فك الضغط والبدء فورًا.
