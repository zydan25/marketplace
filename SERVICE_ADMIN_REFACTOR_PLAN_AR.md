# حالة تنفيذ إدارة الخدمات والـAPI

الفرع: `feat/api-legacy-integration-cleanup`

- إعداد الربطية مبسط إلى API URL وUser ID وUsername وPassword فقط.
- المسارات القياسية ثابتة في عقد الـAPI وتُجهز تلقائيًا ولا تُحرر يدويًا.
- كلمة المرور مشفرة وتركها فارغة عند التعديل يحافظ على القيمة الحالية.
- إدارة الفئات والخدمات والحقول أصبحت موحدة وسهلة.
- مكتبة CheckBox تستخدم أسماء حقول العقد الفعلية مثل `playerid`, `playername`, `zoneid`, `offerid`, `offerkey`, `method`, `solfa`, `num`, `type`, `customer_id`, `placeid`.
- الباقات والفئات والألعاب والمنتجات الرقمية والخيارات العامة قابلة للإضافة والتعديل والتفعيل/الإيقاف والحذف الآمن.
- بيانات الباقة الموسعة الموجودة في النظام القديم محفوظة مع المورد عبر metadata عندما لا يكون لها عمود مستقل في المخطط الحالي.
- التوزيع هو: مزود + ترتيب + CheckBox للخدمات + حفظ، مع اختيار المسار القياسي الصحيح لكل خدمة.
- الحذف لا يكسر السجل التاريخي، وتستخدم الأرشفة عند وجود عمليات مرتبطة.
- طبقة تنفيذ المزود منفصلة عن الطبقة المحاسبية.
- آخر تشغيل ناجح تم التحقق منه شمل Django checks وmigrations وcatalog integrity والاختبارات، إضافة إلى TypeScript وبناء Customer وVendor Web. بعد تغييرات الاختبار الأخيرة يلزم اعتماد تشغيل CI جديد على HEAD النهائي قبل الدمج.

الفرع تجريبي ومستقل ولا يتم دمجه تلقائيًا.
