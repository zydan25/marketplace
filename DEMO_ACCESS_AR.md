# بيانات التجربة

لا يحتوي المستودع على كلمات مرور ثابتة. لتوليد حسابات تجربة محليًا، شغّل أمر Django التالي من مجلد `backend`:

```bash
python3 manage.py seed_demo
```

سيطبع الأمر كلمات مرور عشوائية جديدة في الطرفية. ويمكنك تحديد كلمات مرور خاصة بك قبل التشغيل عبر متغيرات البيئة التالية، مع عدم رفعها إلى GitHub:

```bash
export DEMO_ADMIN_PASSWORD='ضع-كلمة-مرور-قوية-هنا'
export DEMO_VENDOR_PASSWORD='ضع-كلمة-مرور-قوية-هنا'
export DEMO_CUSTOMER_PASSWORD='ضع-كلمة-مرور-قوية-هنا'
python3 manage.py seed_demo
```

حسابات التجربة تستخدم أرقام الهاتف التالية فقط، بينما كلمات المرور تُنشأ عند تشغيل الأمر:

| الدور | رقم الهاتف |
|---|---:|
| المدير | 777000001 |
| التاجر | 777000002 |
| العميل | 777000003 |

يجب تغيير كلمات المرور بعد أول تسجيل دخول وعدم استخدام حسابات التجربة في بيئة الإنتاج.
