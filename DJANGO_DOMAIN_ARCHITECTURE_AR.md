# معمارية Django بعد التفكيك المرحلي — شبيك

تم اعتماد تفكيك نطاقات Django إلى تطبيقات مستقلة دون تغيير الجداول الفيزيائية في المرحلة الحالية.

## التطبيقات

- `accounts`: المستخدمون، المصادقة، التفضيلات والصلاحيات.
- `catalog`: الفئات، المنتجات، الصور، الأصناف، خيارات الكتالوج ومجموعات الأسعار.
- `vendors`: ملفات التجار وطلبات الانضمام وعمليات التاجر.
- `storefront`: الثيمات، أقسام الواجهة، وسائط الواجهة ومحرر المتجر.
- `orders`: الطلبات، عناصر الطلب، طلبات التجار، حالات الطلب، الشحنات، حجوزات المخزون والمدفوعات المرتبطة بدورة الطلب.
- `finance`: المحافظ، حركات المحافظ، دفتر التاجر، السحوبات، أسعار الصرف وشحن المدن والمدفوعات.
- `communication`: الإشعارات، المحادثات، الرسائل، محادثات الطلب والدعم.
- `promotions`: الكوبونات واستخداماتها والإحالات والعناوين وطلبات التمويل وتحويلات الهدايا.

## استراتيجية سلامة قاعدة البيانات

كل تطبيق جديد يستخدم Proxy Models فوق نماذج `marketplace` الحالية. هذا يجعل ownership البرمجي واضحًا من الآن مع بقاء الجداول والأرقام والعلاقات الحالية دون نقل فيزيائي.

لا يتم تغيير `AUTH_USER_MODEL` ولا إعادة تسمية الجداول ضمن هذا الريفاكتور. النقل الفيزيائي لاحقًا يجب أن يكون Migration مستقلة من نوع مدروس، مع نسخ احتياطي واختبار رجوع.

## API

تمت إضافة عقد إصدار مستقل تحت `/api/v2/`:

- `/api/v2/accounts/`
- `/api/v2/catalog/`
- `/api/v2/vendors/`
- `/api/v2/storefront/`
- `/api/v2/orders/`
- `/api/v2/finance/`
- `/api/v2/communication/`
- `/api/v2/promotions/`
- `/api/v2/schema/`

تبقى `/api/` القديمة موجودة كطبقة توافق حتى لا تنكسر الواجهة الحالية.

## Admin وViews

كل نطاق جديد يملك `admin.py` و`views.py` ومسارًا خاصًا في لوحة الإدارة. توجد لوحة HTML موحدة للنطاقات الجديدة، بالإضافة إلى Django Admin الرسمي.

## قواعد التطوير التالية

1. لا يُحذف أي كود legacy ما دام هناك عميل يعتمد عليه.
2. كل domain جديدة تستلم models + admin + API + views + tests في نفس المرحلة.
3. بعد استقرار الـAPI يتم نقل business services من `marketplace` إلى التطبيق المالِك تدريجيًا.
4. بعد اكتمال النقل المنطقي تبدأ مرحلة تنظيف `marketplace` ثم النقل الفيزيائي للجداول عند الحاجة.
