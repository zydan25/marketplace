# نتائج تدقيق الكتالوج

## أسباب الأعطال المؤكدة

1. نموذج Django Product يستخدم `slug` كـ `lookup_field` في `ProductViewSet`، بينما التطبيق يرسل رقم المنتج من بطاقة المنتج إلى `/api/products/{id}/`. لذلك تظهر شاشة «الصنف غير موجود» عند فتح المنتج.
2. `ProductSerializer.get_main_image_url` يعيد `obj.main_image.url` نسبيًا مثل `/media/products/...` دون تحويله إلى URL مطلق، لذلك قد لا تظهر الصور في تطبيق Expo/الويب المؤقت.
3. حقل `Product.images` مخزن كـ JSON، لكن `lib/product-api.ts` يفترض أن كل عنصر صورة كائن يحوي `url`. إذا كانت البيانات القديمة نصوصًا أو مفاتيح مختلفة، يتم إسقاطها.
4. شاشة التاجر الحالية تضيف الاسم وSKU والسعر والمخزون فقط ولا ترفع صورًا ولا تحفظ الوصف والتفاصيل والألوان والمقاسات والهاشتاجات وسعر التخفيض والفئة والترند.
5. شاشة تفاصيل المنتج تعرض وصفًا مختصرًا فقط ولا تعرض كل الحقول المطلوبة مثل السعر قبل/بعد الخصم، الشحن، الخامة، الألوان، المقاسات، التقييم والمراجعات، سياسات الإرجاع، معلومات التاجر، والعمليات المشابهة.
6. `getProduct` في `lib/product-api.ts` يستدعي رقم المنتج مباشرة، وهو متعارض مع lookup slug الحالي.

## الحقول الموجودة أصلًا في Django Product

`vendor`, `categories`, `sku`, `name`, `slug`, `description`, `price`, `sale_price`, `currency`, `stock`, `colors`, `sizes`, `hashtags`, `details`, `main_image`, `images`, `rating`, `reviews_count`, `is_published`, `is_trending`.

## المطلوب التالي

إزالة تعارض الرقم/slug، تطبيع الصور النصية والكائنات وتحويل الصور النسبية إلى URL مطلق، توسيع Serializer ليعيد الخامة والشحن والحقول التفصيلية، بناء محرر منتج حقيقي للتاجر مع ImagePicker ورفع الصور، ثم إعادة بناء تفاصيل المنتج والسلة والتجار والبحث بالعربية. بعد ذلك يجب إضافة عمليات الرصيد وسجل العمليات والإيصالات وتحديث التصميم من الإدارة قبل إعادة بناء APK نهائي.
