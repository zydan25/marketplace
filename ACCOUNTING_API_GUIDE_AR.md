# عقد تكامل API المالي والمحاسبي — Marketplace

هذه الطبقة هي مصدر الحقيقة المالية. التطبيق العميل/التاجر لا يحسب الرصيد ولا العمولة ولا الاسترداد ولا حالة الـescrow؛ يرسل طلب خدمة إلى Django ويعرض النتيجة فقط.

## 1) المبدأ

كل رصيد يظهر من `JournalEntry + JournalLine` عبر الحساب المرتبط بالمحفظة. لا توجد قيمة رصيد محاسبي مستقلة يجب على التطبيق تحديثها.

الأنواع الأساسية للمحافظ:

- `customer`: رصيد العميل القابل للاستخدام.
- `vendor_pending`: مستحقات التاجر المحجوزة حتى اكتمال الاستلام.
- `vendor_available`: المستحقات التي أصبحت متاحة للسحب.
- `withdrawal_hold`: مبلغ طلب السحب المحجوز مؤقتًا.

الحسابات الرئيسية لا تقبل القيود؛ القيود دائمًا على الحسابات الفرعية.

## 2) الرصيد وكشف الحساب

`GET /api/v2/accounting/wallets/me/balance/?currency=YER`

يرجع Django الرصيد المحسوب من القيود في نفس لحظة الطلب.

`GET /api/v2/accounting/me/report/?currency=YER`

يرجع المحافظ + الأرصدة + كشف الحساب + مصادر القيود.

`GET /api/v2/accounting/wallets/me/statement/?currency=YER`

يرجع الحركات المحاسبية مع رقم القيد وتاريخ الحركة والمصدر.

## 3) التحويل بين عميلين

`POST /api/v2/accounting/transfers/`

```json
{
  "recipient": "9677xxxxxxx",
  "amount": "500.00",
  "currency": "YER",
  "note": "تحويل رصيد",
  "idempotency_key": "client-transfer-unique-key"
}
```

القيد:

`مدين: حساب محفظة المرسل`

`دائن: حساب محفظة المستلم`

قبل الترحيل يتحقق الخادم من الرصيد. إذا لم يكفِ الرصيد يرفض العملية ولا ينشئ القيد.

## 4) الهدية

`POST /api/v2/accounting/gifts/`

نفس البنية تقريبًا:

```json
{
  "recipient": "9677xxxxxxx",
  "amount": "100.00",
  "currency": "YER",
  "message": "هدية",
  "idempotency_key": "gift-unique-key"
}
```

القيد أيضًا تحويل مزدوج بين محفظتي العميلين، مع مصدر قيد `gift`.

## 5) إنشاء الطلب

`POST /api/v2/orders/orders/`

الخادم هو الذي يقوم بالتالي:

1. يتحقق من العميل وصلاحياته.
2. يتحقق من المنتجات والتاجر والمخزون والكميات.
3. يحسب الأسعار والخصومات والشحن والعمولة.
4. يحسب إجمالي الطلب على الخادم، ولا يثق في إجمالي يرسله التطبيق.
5. يقرأ رصيد محفظة العميل من القيود.
6. يرفض الطلب إذا لم يكن الرصيد كافيًا.
7. ينشئ قيد تمويل الطلب.
8. يسجل صافي التاجر في `vendor_pending`.
9. يسجل عمولة المنصة في حساب الإيرادات.
10. يرجع للتطبيق رسالة قبول وبيانات الطلب ورقم القيد.

قيد الطلب:

`مدين: محفظة العميل`

`دائن: مستحقات التاجر المعلقة`

`دائن: إيرادات عمولة المنصة`

## 6) تأكيد الاستلام

`POST /api/v2/orders/orders/{id}/confirm_received/`

بعد التحقق من وصول الطلب وعدم وجود اعتراض معلق، ينقل الخادم مستحقات التاجر:

`مدين: vendor_pending`

`دائن: vendor_available`

ولا يغيّر التطبيق أي رصيد محليًا كحقيقة مالية.

## 7) الاعتراض على قطعة / الاسترداد

فتح الاعتراض لا يعني الاسترداد تلقائيًا. الخادم يسجل حالة الاعتراض ويربطها بالطلب والقطعة.

عند اتخاذ قرار الاسترداد، يجب أن يكون الاسترداد قيدًا عكسيًا للحركة الأصلية:

- إذا كانت المستحقات ما زالت معلقة: `مدين vendor_pending` + `مدين عمولة المنصة` مقابل `دائن محفظة العميل`.
- إذا كانت قد أصبحت متاحة للتاجر: `مدين vendor_available` + `مدين عمولة المنصة` مقابل `دائن محفظة العميل`.

لا يتم تعديل القيد الأصلي أو حذفه؛ تتم المعالجة بقيد تسوية جديد مرتبط بالطلب/القطعة.

## 8) تعديل الطلب قبل تأكيده

التعديل المالي ليس تحديثًا صامتًا للرصيد.

التسلسل الصحيح:

1. يتحقق الخادم أن الطلب ما زال قابلًا للتعديل.
2. يعيد تسعير الطلب والمخزون والشحن والعمولة.
3. يعكس قيد التمويل السابق بقيد عكسي.
4. يتحقق من الرصيد الجديد.
5. ينشئ قيد تمويل جديد بالقيمة الجديدة.
6. يحفظ أرقام القيود في بيانات الطلب.

## 9) إلغاء الطلب

في حالة إلغاء طلب ما زالت أمواله محجوزة، يتم إنشاء قيد عكسي للتمويل وإرجاع قيمة العميل حسب الجزء المحرر/المعلق، ولا يتم حذف القيود.

## 10) سحب التاجر

`POST /api/v2/accounting/withdrawals/`

أولًا:

`مدين vendor_available`

`دائن withdrawal_hold`

ثم اعتماد الطلب:

`POST /api/v2/accounting/withdrawals/{id}/approve/`

وعند الصرف:

`POST /api/v2/accounting/withdrawals/{id}/pay/`

`مدين withdrawal_hold`

`دائن الصندوق`

وعند الرفض:

`مدين withdrawal_hold`

`دائن vendor_available`

## 11) السندات والقيود

`POST /api/v2/accounting/vouchers/receipt/`

لإنشاء سند قبض.

`POST /api/v2/accounting/vouchers/payment/`

لإنشاء سند صرف.

`POST /api/v2/accounting/journals/post/`

للقيد اليدوي من الإدارة فقط.

## 12) الأمن والتكامل

التطبيق يرسل Token للمصادقة ويستدعي API فقط.

التطبيق لا يقرر:

- الرصيد.
- قبول الطلب.
- قيمة الطلب النهائية.
- العمولة.
- رصيد التاجر المعلق.
- أهلية السحب.
- قيمة الاسترداد.
- الحسابات المحاسبية.
- أرقام القيود.

الخادم هو الذي يقرر ويرجع نتيجة واضحة، مع `journal` أو مصدر العملية عند وجود حركة مالية.

## 13) Idempotency

كل عملية مالية حساسة يجب أن ترسل `idempotency_key` فريدًا عندما يدعم endpoint ذلك، لمنع تكرار الخصم عند إعادة إرسال نفس الطلب بسبب انقطاع الشبكة.

## 14) العقد المقترح للتطبيق

الطبقة البرمجية في التطبيق يفضل أن تحتوي خدمات مثل:

`getBalance()`

`getStatement()`

`createOrder()`

`confirmOrder()`

`transfer()`

`sendGift()`

`requestWithdrawal()`

كل دالة تستدعي Django وتتعامل مع `success/error` فقط، ولا تعيد حساب أي قيمة مالية محليًا.
