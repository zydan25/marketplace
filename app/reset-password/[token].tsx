import { useLocalSearchParams, router } from "expo-router";
import { useState } from "react";
import { Alert, Pressable, StyleSheet, Text, TextInput, View } from "react-native";
import { ScreenContainer } from "@/components/screen-container";
import { confirmPasswordReset } from "@/lib/password-reset-api";
export default function ResetPasswordScreen(){const {token}=useLocalSearchParams<{token:string}>();const [password,setPassword]=useState("");const [confirm,setConfirm]=useState("");const [saving,setSaving]=useState(false);async function submit(){if(password.length<6){Alert.alert("كلمة المرور ضعيفة","استخدم 6 أحرف على الأقل.");return;}if(password!==confirm){Alert.alert("تأكيد غير مطابق","أعد كتابة كلمة المرور نفسها.");return;}try{setSaving(true);await confirmPasswordReset(String(token||""),password);Alert.alert("تمت الاستعادة","تم تغيير كلمة المرور بنجاح.",[{text:"تسجيل الدخول",onPress:()=>router.replace("/login" as never)}]);}catch(e){Alert.alert("تعذر الاستعادة",e instanceof Error?e.message:"الرابط غير صالح أو منتهي.");}finally{setSaving(false);}}return <ScreenContainer className="bg-[#F7F7F7]"><View style={styles.wrap}><Text style={styles.title}>استعادة كلمة المرور</Text><Text style={styles.sub}>أدخل كلمة المرور الجديدة ثم أكدها.</Text><TextInput secureTextEntry value={password} onChangeText={setPassword} placeholder="كلمة المرور الجديدة" style={styles.input}/><TextInput secureTextEntry value={confirm} onChangeText={setConfirm} placeholder="تأكيد كلمة المرور" style={styles.input}/><Pressable disabled={saving} onPress={submit} style={[styles.button,saving&&styles.disabled]}><Text style={styles.buttonText}>{saving?"جارٍ الحفظ...":"تغيير كلمة المرور"}</Text></Pressable></View></ScreenContainer>}
const styles=StyleSheet.create({wrap:{padding:22,flex:1,justifyContent:"center",maxWidth:430,width:"100%",alignSelf:"center"},title:{fontSize:24,fontWeight:"900",textAlign:"right",color:"#111"},sub:{fontSize:12,color:"#777",textAlign:"right",marginTop:8,marginBottom:22},input:{height:48,borderWidth:1,borderColor:"#DDD",borderRadius:12,backgroundColor:"#FFF",paddingHorizontal:12,textAlign:"right",marginBottom:10},button:{height:48,borderRadius:24,backgroundColor:"#111",alignItems:"center",justifyContent:"center"},buttonText:{color:"#FFF",fontSize:13,fontWeight:"900"},disabled:{opacity:.55}});