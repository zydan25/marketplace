import MaterialIcons from "@expo/vector-icons/MaterialIcons";
import * as Clipboard from "expo-clipboard";
import { ActivityIndicator, Alert, FlatList, Image, ScrollView, StyleSheet, Text, TouchableOpacity, View } from "react-native";
import { router, useLocalSearchParams } from "expo-router";
import { useEffect, useMemo, useState } from "react";
import { ProductCard } from "@/components/product-card";
import { ScreenContainer } from "@/components/screen-container";
import { useCart } from "@/lib/cart-context";
import { formatYER } from "@/lib/catalog";
import { getProduct, type StoreProduct } from "@/lib/product-api";
import { ShareButton } from "@/components/share-button";

export default function ProductDetailsScreen() {
  const { id } = useLocalSearchParams<{ id: string }>();
  const { addItem } = useCart();
  const [product, setProduct] = useState<StoreProduct | null>(null);
  const [similar, setSimilar] = useState<StoreProduct[]>([]);
  const [loading, setLoading] = useState(true);
  const [color, setColor] = useState("");
  const [size, setSize] = useState("");
  const [quantity, setQuantity] = useState(1);
  const [image, setImage] = useState(0);

  useEffect(() => {
    getProduct(id).then(({ product: item, similar: related }) => {
      setProduct(item); setSimilar(related);
      const firstVariant = item.variants.find((variant) => variant.isActive && variant.availableStock > 0);
      setColor(firstVariant?.color ?? item.colors[0]?.name ?? "");
      setSize(firstVariant?.size ?? item.sizes[0]?.label ?? "");
    }).catch(() => setProduct(null)).finally(() => setLoading(false));
  }, [id]);

  const selectedVariant = useMemo(() => product?.variants.find((variant) => variant.isActive && variant.color === color && variant.size === size), [color, product, size]);
  const selectedPrice = selectedVariant?.effectivePrice ?? product?.price ?? 0;
  const baseStock = product?.sizes.reduce((total, item) => total + Number(item.stock || 0), 0) ?? 0;
  const availableStock = product && product.variants.length > 0 ? (selectedVariant?.availableStock ?? 0) : baseStock;
  const canAdd = availableStock > 0 && quantity <= availableStock;

  if (loading) return <ScreenContainer edges={["top", "bottom"]}><View style={s.center}><ActivityIndicator color="#E60023" /></View></ScreenContainer>;
  if (!product) return <ScreenContainer edges={["top", "bottom"]}><View style={s.center}><Text style={s.emptyText}>الصنف غير متاح حاليًا.</Text><TouchableOpacity style={s.retry} onPress={() => router.back()}><Text style={s.retryText}>العودة</Text></TouchableOpacity></View></ScreenContainer>;

  const code = product.productCode || `TDS-${product.id}`;
  const copyCode = async () => { await Clipboard.setStringAsync(code); Alert.alert("تم النسخ", `تم نسخ رقم الصنف: ${code}`); };
  const availableColors = product.variants.length ? [...new Set(product.variants.filter((variant) => variant.isActive).map((variant) => variant.color).filter(Boolean))] : product.colors.map((item) => item.name);
  const availableSizes = product.variants.length ? [...new Set(product.variants.filter((variant) => variant.isActive).map((variant) => variant.size).filter(Boolean))] : product.sizes.map((item) => item.label);

  return <ScreenContainer edges={["top", "left", "right"]} className="bg-white"><View style={s.header}><TouchableOpacity onPress={() => router.back()}><MaterialIcons name="arrow-forward" size={24} /></TouchableOpacity><Text style={s.headerTitle}>تفاصيل المنتج</Text><View style={{ flexDirection: "row", gap: 10 }}><ShareButton type="product" id={id} title={product.name} /><TouchableOpacity style={s.iconButton} onPress={() => router.push("/bag" as never)}><MaterialIcons name="shopping-bag" size={23} /></TouchableOpacity></View></View><ScrollView contentContainerStyle={s.content} showsVerticalScrollIndicator>{product.images.length ? <><Image source={{ uri: product.images[image]?.url }} style={s.photo} /><FlatList horizontal inverted data={product.images} keyExtractor={(item) => String(item.id)} contentContainerStyle={s.thumbs} showsHorizontalScrollIndicator={false} renderItem={({ item, index }) => <TouchableOpacity onPress={() => setImage(index)} style={[s.thumb, image === index && s.thumbActive]}><Image source={{ uri: item.url }} style={s.thumbImage} /></TouchableOpacity>} /></> : <View style={s.emptyPhoto}><MaterialIcons name="image-not-supported" size={42} color="#AAA" /></View>}<View style={s.body}><Text style={s.category}>{product.category}</Text><Text style={s.title}>{product.name}</Text><View style={s.pricing}><Text style={s.price}>{formatYER(selectedPrice)}</Text>{selectedPrice < product.originalPrice ? <Text style={s.oldPrice}>{formatYER(product.originalPrice)}</Text> : null}</View><View style={s.codeRow}><TouchableOpacity onPress={copyCode} style={s.copy}><MaterialIcons name="content-copy" size={15} color="#E60023" /><Text style={s.copyText}>نسخ</Text></TouchableOpacity><View><Text style={s.code}>رقم الصنف: {selectedVariant?.sku || code}</Text><Text style={s.hint}>اضغط لنسخ رقم الصنف</Text></View></View></View>{availableColors.length>0&&<Choice title={`اللون: ${color || "الافتراضي"}`}>{availableColors.map((name) => <TouchableOpacity key={name} onPress={() => { setColor(name); setQuantity(1); }} style={[s.color, color === name && s.activeColor, { backgroundColor: product.colors.find((item) => item.name === name)?.hex ?? "#E5E5E5" }]} />)}</Choice>}{availableSizes.length>0&&<Choice title={`المقاس: ${size || "الافتراضي"}`}>{availableSizes.map((label) => { const variant = product.variants.find((item) => item.isActive && item.color === color && item.size === label); const inStock = product.variants.length ? Boolean(variant && variant.availableStock > 0) : baseStock > 0; return <TouchableOpacity key={label} disabled={!inStock} onPress={() => { setSize(label); setQuantity(1); }} style={[s.size, size === label && s.activeSize, !inStock && s.disabledSize]}><Text style={size === label ? s.activeSizeText : inStock ? s.sizeText : s.disabledSizeText}>{label}</Text></TouchableOpacity>; })}</Choice>}<Choice title="الكمية"><View style={s.quantity}><TouchableOpacity disabled={quantity <= 1} onPress={() => setQuantity(Math.max(1, quantity - 1))}><MaterialIcons name="remove" size={20} color={quantity <= 1 ? "#BBB" : "#111"} /></TouchableOpacity><Text style={s.number}>{quantity}</Text><TouchableOpacity disabled={quantity >= availableStock} onPress={() => setQuantity(Math.min(availableStock, quantity + 1))}><MaterialIcons name="add" size={20} color={quantity >= availableStock ? "#BBB" : "#111"} /></TouchableOpacity></View><Text style={s.stockHint}>{availableStock > 0 ? `متوفر ${availableStock}` : "غير متوفر حاليًا"}</Text></Choice><View style={s.body}><Text style={s.section}>تفاصيل المنتج</Text><Text style={s.description}>{product.description || "منتج مختار بعناية من متجر موثوق."}</Text>{product.material ? <Text style={s.detailLine}>الخامة: {product.material}</Text> : null}{product.details ? <Text style={s.detailLine}>{product.details}</Text> : null}</View><View style={s.infoPanel}><View style={s.infoRow}><MaterialIcons name="local-shipping" size={22} color="#168451" /><Text style={s.infoText}>{product.shippingNote || "شحن آمن إلى عنوانك"}</Text></View><View style={s.infoRow}><MaterialIcons name="assignment-return" size={22} color="#555" /><Text style={s.infoText}>{product.returnPolicy || "تطبق سياسة الإرجاع الخاصة بالمتجر"}</Text></View></View><TouchableOpacity style={s.vendorPanel} onPress={() => router.push(`/store/${product.vendor.slug}` as never)}><View style={s.vendorCopy}><Text style={s.vendorName}>{product.vendor.name}</Text><Text style={s.vendorDescription}>{product.vendor.description || "عرض المتجر والمنتجات والتقييمات"}</Text></View><MaterialIcons name="storefront" size={32} color="#111" /></TouchableOpacity><View style={s.related}><Text style={s.section}>ربما يعجبك أيضًا</Text><FlatList horizontal inverted data={similar} keyExtractor={(item) => item.id} contentContainerStyle={s.relatedScroller} showsHorizontalScrollIndicator={false} renderItem={({ item }) => <View style={s.relatedCard}><ProductCard product={item} /></View>} /></View></ScrollView><View style={s.bottom}><TouchableOpacity disabled={!canAdd} style={[s.add, !canAdd && s.addDisabled]} onPress={() => { addItem(product, color, size, quantity); Alert.alert("تمت الإضافة", "أصبح الصنف في حقيبتك."); }}><Text style={s.addText}>{canAdd ? `أضف إلى الحقيبة · ${formatYER(selectedPrice * quantity)}` : "المخزون غير متاح"}</Text></TouchableOpacity></View></ScreenContainer>;
}
function Choice({ title, children }: { title: string; children: React.ReactNode }) { return <View style={s.choice}><Text style={s.choiceTitle}>{title}</Text><View style={s.options}>{children}</View></View>; }
const s=StyleSheet.create({header:{height:54,paddingHorizontal:16,flexDirection:"row",alignItems:"center",justifyContent:"space-between",borderBottomWidth:1,borderColor:"#F5F5F5",backgroundColor:"#FFF"},headerTitle:{fontSize:16,fontWeight:"900",color:"#111"},iconButton:{width:40,height:40,justifyContent:"center",alignItems:"center"},center:{flex:1,alignItems:"center",justifyContent:"center",padding:20},emptyText:{fontSize:14,color:"#555",textAlign:"center"},retry:{marginTop:12,paddingHorizontal:20,paddingVertical:10,backgroundColor:"#111",borderRadius:20},retryText:{color:"#FFF",fontWeight:"800"},content:{paddingBottom:100},photo:{width:"100%",height:380,backgroundColor:"#F9F9F9"},thumbs:{padding:12,gap:8},thumb:{width:56,height:72,borderWidth:2,borderColor:"transparent",borderRadius:8,overflow:"hidden"},thumbActive:{borderColor:"#111"},thumbImage:{width:"100%",height:"100%"},emptyPhoto:{height:280,alignItems:"center",justifyContent:"center",backgroundColor:"#F5F5F5"},body:{padding:16,alignItems:"flex-end"},category:{color:"#777",fontSize:11,fontWeight:"600"},title:{fontSize:18,fontWeight:"900",color:"#111",textAlign:"right",marginTop:4,lineHeight:26},pricing:{width:"100%",flexDirection:"row-reverse",gap:8,alignItems:"center",marginTop:12},price:{color:"#111",fontSize:22,fontWeight:"900"},oldPrice:{color:"#999",textDecorationLine:"line-through",fontSize:13},codeRow:{marginTop:16,flexDirection:"row",gap:12,alignItems:"center",padding:12,backgroundColor:"#F9F9F9",borderRadius:8,width:"100%",justifyContent:"flex-end"},code:{fontWeight:"700",fontSize:12,color:"#333",textAlign:"right"},hint:{color:"#777",fontSize:10,marginTop:2,textAlign:"right"},copy:{paddingHorizontal:10,paddingVertical:6,flexDirection:"row-reverse",gap:4,backgroundColor:"#FFF",borderWidth:1,borderColor:"#EFEFEF",borderRadius:16,alignItems:"center"},copyText:{fontSize:10,color:"#111",fontWeight:"700"},choice:{padding:16,borderTopWidth:1,borderColor:"#F5F5F5",alignItems:"flex-end"},choiceTitle:{fontSize:14,fontWeight:"900",color:"#111"},options:{flexDirection:"row-reverse",flexWrap:"wrap",gap:10,marginTop:12},color:{width:36,height:36,borderRadius:18,borderWidth:2,borderColor:"transparent"},activeColor:{borderColor:"#111"},size:{minWidth:48,paddingHorizontal:16,paddingVertical:8,borderWidth:1,borderColor:"#EFEFEF",borderRadius:20,alignItems:"center",backgroundColor:"#F9F9F9"},activeSize:{backgroundColor:"#111",borderColor:"#111"},sizeText:{fontSize:12,color:"#333",fontWeight:"600"},activeSizeText:{fontSize:12,color:"#FFF",fontWeight:"700"},disabledSize:{backgroundColor:"#F1F1F1",borderColor:"#EEE"},disabledSizeText:{fontSize:12,color:"#AAA",fontWeight:"600"},quantity:{width:120,flexDirection:"row",justifyContent:"space-between",alignItems:"center",borderWidth:1,borderColor:"#EFEFEF",borderRadius:24,padding:4,backgroundColor:"#F9F9F9",alignSelf:"flex-end"},number:{fontWeight:"900",fontSize:14,color:"#111"},stockHint:{marginTop:7,color:"#777",fontSize:11,textAlign:"right"},section:{fontSize:15,fontWeight:"900",color:"#111",marginBottom:8},description:{textAlign:"right",lineHeight:22,fontSize:13,color:"#555"},detailLine:{width:"100%",textAlign:"right",lineHeight:24,fontSize:13,color:"#333",marginTop:8},infoPanel:{backgroundColor:"#F8FBF8",padding:16,borderTopWidth:1,borderBottomWidth:1,borderColor:"#E8F1E8",gap:12},infoRow:{flexDirection:"row-reverse",alignItems:"center",gap:10},infoText:{flex:1,textAlign:"right",color:"#333",fontSize:13},vendorPanel:{flexDirection:"row-reverse",alignItems:"center",gap:12,margin:16,padding:14,borderRadius:10,backgroundColor:"#FFF",borderWidth:1,borderColor:"#E8E8E8"},vendorCopy:{flex:1,alignItems:"flex-end"},vendorName:{fontWeight:"900",fontSize:15,color:"#111"},vendorDescription:{color:"#777",fontSize:11,marginTop:4,textAlign:"right"},related:{padding:16,borderTopWidth:1,borderColor:"#F5F5F5",alignItems:"flex-end"},relatedScroller:{paddingLeft:4},relatedCard:{width:140,marginTop:12,marginLeft:12},bottom:{position:"absolute",bottom:0,left:0,right:0,padding:16,backgroundColor:"#FFF",borderTopWidth:1,borderColor:"#F5F5F5"},add:{height:48,borderRadius:24,backgroundColor:"#111",alignItems:"center",justifyContent:"center"},addDisabled:{backgroundColor:"#C9C9C9"},addText:{color:"#FFF",fontWeight:"800",fontSize:14}});
