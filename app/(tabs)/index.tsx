import MaterialIcons from "@expo/vector-icons/MaterialIcons";
import * as Linking from "expo-linking";
import { router } from "expo-router";
import { useCallback, useMemo, useState } from "react";
import { FlatList, Image, Pressable, RefreshControl, ScrollView, StyleSheet, Text, useWindowDimensions, View } from "react-native";
import { ProductCard } from "@/components/product-card";
import { ShopHeader } from "@/components/shop-header";
import { useCategories } from "@/hooks/use-categories";
import { useProducts } from "@/hooks/use-products";
import { useStorefront } from "@/hooks/use-storefront";
import type { StoreProduct } from "@/lib/product-api";
import type { StorefrontCard, StorefrontCircle, StorefrontSlide, StorefrontTab, StorefrontTheme } from "@/lib/storefront-api";

type StorefrontSectionConfig = Record<string, unknown>;
function token(theme: StorefrontTheme | null, key: string, fallback: string) { const value = theme?.tokens?.[key]; return typeof value === "string" ? value : fallback; }

export default function StoreScreen() {
  const { width } = useWindowDimensions();
  const { products, loading: productsLoading, refresh: refreshProducts } = useProducts();
  const { tabs: sections, theme, loading: storefrontLoading, refresh: refreshStorefront } = useStorefront();
  const { categories, loading: categoriesLoading, refresh: refreshCategories } = useCategories();
  const [refreshing, setRefreshing] = useState(false);
  const colors = useMemo(() => ({ background: token(theme, "background", "#FFFFFF"), surface: token(theme, "surface", "#FFFFFF"), text: token(theme, "text", "#111111"), muted: token(theme, "muted", "#777777"), border: token(theme, "border", "#EEEEEE"), primary: token(theme, "primary", "#E60023") }), [theme]);
  const visibleSections = useMemo(() => sections.filter((section) => section.isActive).sort((a, b) => a.sortOrder - b.sortOrder), [sections]);
  const globalSearchPlaceholder = useMemo(() => visibleSections.find((section) => section.searchPlaceholder)?.searchPlaceholder ?? "ابحث عن منتج أو متجر", [visibleSections]);
  const refresh = useCallback(async () => { setRefreshing(true); try { await Promise.all([refreshProducts(), refreshStorefront(), refreshCategories()]); } finally { setRefreshing(false); } }, [refreshCategories, refreshProducts, refreshStorefront]);
  const renderedSections = visibleSections.length > 0 ? visibleSections : fallbackSections(products, categories);
  return <View style={[styles.page, { backgroundColor: colors.background }]}><ShopHeader placeholder={globalSearchPlaceholder} /><FlatList data={products} keyExtractor={(item) => item.id} numColumns={2} contentInsetAdjustmentBehavior="automatic" style={styles.list} contentContainerStyle={styles.listContent} columnWrapperStyle={styles.productRow} showsVerticalScrollIndicator refreshControl={<RefreshControl refreshing={refreshing || productsLoading || storefrontLoading || categoriesLoading} onRefresh={refresh} />} ListHeaderComponent={<View style={styles.headerContent}>{renderedSections.map((section) => <StorefrontSection key={String(section.id)} section={section} products={products} colors={colors} width={width} />)}</View>} renderItem={({ item }) => <View style={styles.productCell}><ProductCard product={item} /></View>} ListEmptyComponent={<EmptyState loading={productsLoading || storefrontLoading} colors={colors} />} /></View>;
}
function fallbackSections(products: StoreProduct[], categories: { name: string }[]): StorefrontTab[] { const firstImage=products[0]?.images[0]?.url ?? ""; return [{id:"fallback",type:"hero",title:"الرئيسية",searchPlaceholder:"ابحث عن منتج أو متجر",isActive:true,sortOrder:0,slides:firstImage?[{id:"hero",title:"اختيارات مميزة",subtitle:"تسوّق أحدث المنتجات",imageUrl:firstImage,url:"/collection",badge:"",visible:true,isActive:true,sortOrder:0,ctaLabel:"تسوّق الآن"} satisfies StorefrontSlide]:[],circles:categories.slice(0,12).map((category,index)=>({id:String(index),title:category.name,targetCategory:category.name,imageUrl:"",url:`/collection?category=${encodeURIComponent(category.name)}`,visible:true,isActive:true,sortOrder:index} satisfies StorefrontCircle)),cards:[] as StorefrontCard[],actions:[],promo:{enabled:false,flashTitle:"",flashSubtitle:"",flashMode:"",freeShippingTitle:"",freeShippingSubtitle:"",freeShippingCategory:""},config:{showGlobalGrid:true}}]; }
function StorefrontSection({ section, products, colors, width }: { section: StorefrontTab; products: StoreProduct[]; colors: Record<string,string>; width: number }) {
  const config=(section.config ?? {}) as StorefrontSectionConfig; const filtered=filterProducts(products,config); const sectionType=section.type.toLowerCase(); const isServices=sectionType==="product_grid" && String(config.source ?? "").toLowerCase()==="services";
  return <View style={styles.section}>{(sectionType==="hero"||sectionType==="banner"||section.slides.length>0)&&section.slides.length>0?<HeroBlock slides={section.slides} width={width}/>:null}{section.circles.length>0?<CircleBlock circles={section.circles} colors={colors}/>:null}{isServices&&section.cards.length>0?<ServiceCardBlock cards={section.cards} colors={colors}/>:null}{!isServices&&(sectionType==="trend"||section.cards.length>0)&&section.cards.length>0?<CardsBlock cards={section.cards}/>:null}{section.promo?.enabled?<PromoBlock promo={section.promo}/>:null}{section.actions.length>0?<ActionBlock actions={section.actions}/>:null}{!isServices&&(sectionType==="product_grid"||config.showProducts===true||config.showGlobalGrid===true)?<ProductStrip title={section.title} items={filtered} mode={String(config.cardMode ?? "horizontal")}/>:null}</View>;
}
function HeroBlock({ slides, width }: { slides: StorefrontSlide[]; width: number }) { const slide=slides.find((item)=>item.visible!==false&&item.isActive!==false)??slides[0]; if(!slide?.imageUrl)return null; return <Pressable style={[styles.hero,{height:Math.min(460,Math.max(260,width*0.68))}]} onPress={()=>navigateUrl(slide.url)} accessibilityRole="button"><Image source={{uri:slide.imageUrl}} style={styles.heroImage} resizeMode="cover"/><View style={styles.heroShade}/><View style={styles.heroText}>{slide.badge?<Text style={styles.badge}>{slide.badge}</Text>:null}<Text style={styles.heroTitle}>{slide.title}</Text>{slide.subtitle?<Text style={styles.heroSubtitle}>{slide.subtitle}</Text>:null}{slide.ctaLabel?<View style={styles.heroButton}><Text style={styles.heroButtonText}>{slide.ctaLabel}</Text><MaterialIcons name="arrow-back" size={16} color="#111"/></View>:null}</View></Pressable>; }
function CircleBlock({ circles, colors }: { circles: StorefrontCircle[]; colors: Record<string,string> }) { const visible=circles.filter((circle)=>circle.visible!==false&&circle.isActive!==false); if(!visible.length)return null; return <View style={styles.circleArea}><ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={styles.circleScroller}>{visible.map((circle)=><Pressable key={circle.id} style={styles.circleItem} onPress={()=>navigateUrl(circle.url||`/collection?category=${encodeURIComponent(circle.targetCategory||circle.title)}`)}><View style={[styles.circleImage,{borderColor:colors.border,backgroundColor:colors.surface}]}>{circle.imageUrl?<Image source={{uri:circle.imageUrl}} style={StyleSheet.absoluteFillObject}/>:<MaterialIcons name="category" size={24} color={colors.muted}/>}</View><Text numberOfLines={1} style={[styles.circleText,{color:colors.text}]}>{circle.title}</Text></Pressable>)}</ScrollView></View>; }
function ServiceCardBlock({ cards, colors }: { cards: StorefrontCard[]; colors: Record<string,string> }) { const visible=cards.filter((card)=>card.visible!==false&&card.isActive!==false); return <View style={styles.cardsWrap}><View style={styles.sectionHeader}><Text style={[styles.sectionTitle,{color:colors.text}]}>الخدمات</Text><Pressable onPress={()=>router.push("/services" as never)}><Text style={[styles.seeAll,{color:colors.muted}]}>عرض الكل</Text></Pressable></View><ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={styles.cardsScroller}>{visible.map((card)=><Pressable key={card.id} style={[styles.contentCard,{backgroundColor:colors.surface,borderColor:colors.border}]} onPress={()=>navigateUrl(card.url)}><View style={styles.cardImageWrap}>{card.imageUrl?<Image source={{uri:card.imageUrl}} style={styles.cardImage}/>:<Text style={{fontSize:28,color:colors.muted}}>خدمة</Text>}</View><View style={styles.cardBody}><Text style={[styles.cardTitle,{color:colors.text}]}>{card.title}</Text>{card.subtitle?<Text numberOfLines={2} style={[styles.cardSubtitle,{color:colors.muted}]}>{card.subtitle}</Text>:null}</View></Pressable>)}</ScrollView></View>; }
function CardsBlock({ cards }: { cards: StorefrontCard[] }) { const visible=cards.filter((card)=>card.visible!==false&&card.isActive!==false); if(!visible.length)return null; return <View style={styles.cardsWrap}><ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={styles.cardsScroller}>{visible.map((card)=><Pressable key={card.id} style={styles.contentCard} onPress={()=>navigateUrl(card.url)}>{card.imageUrl?<Image source={{uri:card.imageUrl}} style={styles.cardImage}/>:<View style={styles.cardImageFallback}><MaterialIcons name="image" size={28} color="#aaa"/></View>}<View style={styles.cardBody}>{card.badge?<Text style={styles.cardBadge}>{card.badge}</Text>:null}<Text style={styles.cardTitle}>{card.title}</Text>{card.subtitle?<Text numberOfLines={2} style={styles.cardSubtitle}>{card.subtitle}</Text>:null}</View></Pressable>)}</ScrollView></View>; }
function PromoBlock({ promo }: { promo: NonNullable<StorefrontTab["promo"]> }) { return <View style={styles.promo}><View style={styles.promoItem}><MaterialIcons name="bolt" size={19} color="#111"/><View><Text style={styles.promoTitle}>{promo.flashTitle}</Text><Text style={styles.promoSub}>{promo.flashSubtitle}</Text></View></View><View style={styles.promoDivider}/><View style={styles.promoItem}><MaterialIcons name="local-shipping" size={19} color="#111"/><View><Text style={styles.promoTitle}>{promo.freeShippingTitle}</Text><Text style={styles.promoSub}>{promo.freeShippingSubtitle}</Text></View></View></View>; }
function ActionBlock({ actions }: { actions: {label:string;url:string;visible:boolean}[] }) { const visible=actions.filter((action)=>action.visible!==false&&action.label.trim()); return <View style={styles.actions}>{visible.map((action,index)=><Pressable key={`${action.label}-${index}`} style={styles.action} onPress={()=>navigateUrl(action.url)}><Text style={styles.actionText}>{action.label}</Text><MaterialIcons name="arrow-back" size={16} color="#111"/></Pressable>)}</View>; }
function ProductStrip({ title, items, mode }: { title:string;items:StoreProduct[];mode:string }) { if(!items.length)return null; return <View style={styles.productStrip}><View style={styles.sectionHeader}><Text style={styles.sectionTitle}>{title}</Text><Pressable onPress={()=>router.push("/collection" as never)}><Text style={styles.seeAll}>عرض الكل</Text></Pressable></View><ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={styles.productStripScroller}>{items.slice(0,12).map((item)=><View key={item.id} style={[styles.stripProduct,mode==="compact"&&styles.stripProductCompact]}><ProductCard product={item}/></View>)}</ScrollView></View>; }
function filterProducts(items:StoreProduct[],config:StorefrontSectionConfig){const ids=Array.isArray(config.productIds)?new Set(config.productIds.map(String)):null;const category=typeof config.category==="string"?config.category:"";const mode=String(config.mode??"");let result=items.filter((item)=>!ids||ids.has(String(item.id))).filter((item)=>!category||item.categories.includes(category));if(mode==="deals")result=result.filter((item)=>item.discountPercent>0);if(mode==="bestsellers")result=result.filter((item)=>item.rating>=4||item.reviews>0);return result;}
function navigateUrl(url:string){const value=url.trim();if(!value)return;if(value.startsWith("/")){router.push(value as never);return;}if(/^https?:\/\//i.test(value))void Linking.openURL(value);}
function EmptyState({ loading, colors }: { loading:boolean;colors:Record<string,string> }){return <View style={styles.empty}><MaterialIcons name="inventory-2" size={44} color={colors.muted}/><Text style={[styles.emptyTitle,{color:colors.text}]}>{loading?"جارٍ تحميل المتجر":"لا توجد منتجات متاحة"}</Text><Text style={[styles.emptyText,{color:colors.muted}]}>يمكن للإدارة التحكم بالأقسام والمنتجات من لوحة المنصة.</Text></View>;}
const styles=StyleSheet.create({page:{flex:1},list:{flex:1},listContent:{paddingBottom:110},headerContent:{width:"100%",maxWidth:1180,alignSelf:"center"},section:{width:"100%"},hero:{marginHorizontal:16,borderRadius:18,overflow:"hidden",backgroundColor:"#f5f5f5",position:"relative",marginTop:12},heroImage:{width:"100%",height:"100%"},heroShade:{...StyleSheet.absoluteFillObject,backgroundColor:"rgba(0,0,0,.25)"},heroText:{position:"absolute",right:22,bottom:22,maxWidth:"74%",alignItems:"flex-end"},badge:{backgroundColor:"#fff",color:"#111",fontSize:10,fontWeight:"800",paddingHorizontal:8,paddingVertical:4,borderRadius:99},heroTitle:{color:"#fff",fontSize:25,fontWeight:"900",textAlign:"right",marginTop:9},heroSubtitle:{color:"#fff",fontSize:13,textAlign:"right",marginTop:5},heroButton:{marginTop:12,backgroundColor:"#fff",borderRadius:22,paddingHorizontal:14,paddingVertical:10,flexDirection:"row-reverse",alignItems:"center",gap:6},heroButtonText:{color:"#111",fontSize:12,fontWeight:"800"},circleArea:{paddingVertical:18},circleScroller:{paddingHorizontal:16,gap:18},circleItem:{width:68,alignItems:"center"},circleImage:{width:62,height:62,borderRadius:31,overflow:"hidden",alignItems:"center",justifyContent:"center",borderWidth:1},circleText:{fontSize:11,fontWeight:"700",marginTop:7,maxWidth:68,textAlign:"center"},cardsWrap:{paddingVertical:10},cardsScroller:{paddingHorizontal:16,gap:12},contentCard:{width:250,borderRadius:16,overflow:"hidden",borderWidth:1},cardImage:{width:"100%",height:145,backgroundColor:"#f5f5f5"},cardImageWrap:{width:"100%",height:145,backgroundColor:"#f5f5f5",alignItems:"center",justifyContent:"center"},cardImageFallback:{width:"100%",height:145,backgroundColor:"#f5f5f5",alignItems:"center",justifyContent:"center"},cardBody:{padding:12,alignItems:"flex-end"},cardBadge:{fontSize:10,color:"#8b5cf6",fontWeight:"800",marginBottom:3},cardTitle:{fontSize:15,fontWeight:"900",textAlign:"right"},cardSubtitle:{fontSize:11,lineHeight:18,textAlign:"right"},promo:{marginHorizontal:16,marginVertical:12,padding:13,borderRadius:14,backgroundColor:"#F7F7F7",flexDirection:"row-reverse",alignItems:"center"},promoItem:{flex:1,flexDirection:"row-reverse",alignItems:"center",gap:8},promoDivider:{width:1,height:30,backgroundColor:"#DDD",marginHorizontal:10},promoTitle:{fontSize:11,fontWeight:"900",color:"#111",textAlign:"right"},promoSub:{fontSize:9,color:"#777",marginTop:3,textAlign:"right"},actions:{paddingHorizontal:16,paddingVertical:8,gap:8},action:{minHeight:44,paddingHorizontal:14,borderRadius:12,backgroundColor:"#F5F5F5",flexDirection:"row-reverse",alignItems:"center",justifyContent:"space-between"},actionText:{fontSize:12,fontWeight:"800",color:"#111"},productStrip:{paddingVertical:12},sectionHeader:{paddingHorizontal:16,flexDirection:"row-reverse",alignItems:"center",justifyContent:"space-between"},sectionTitle:{fontSize:17,fontWeight:"900",color:"#111"},seeAll:{fontSize:11,fontWeight:"800",color:"#777"},productStripScroller:{paddingHorizontal:16,gap:12},stripProduct:{width:150},stripProductCompact:{width:132},productRow:{gap:8,paddingHorizontal:12},productCell:{flex:1,minWidth:0},empty:{alignItems:"center",justifyContent:"center",paddingVertical:80,paddingHorizontal:30},emptyTitle:{fontSize:16,fontWeight:"900",marginTop:10},emptyText:{fontSize:12,textAlign:"center",marginTop:6}});
