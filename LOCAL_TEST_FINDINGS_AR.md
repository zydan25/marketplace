
## تحديث الفحص المحلي

تم تشغيل واجهة العميل على `http://127.0.0.1:8083` وواجهة التاجر على `http://127.0.0.1:8082` مع `EXPO_PUBLIC_DJANGO_API_URL=https://shopik.alattab.site`. استجاب كلا المنفذين HTTP 200. واجهة التاجر المحلية تعرض حاليًا نفس شاشة المتجر العامة لأن التوجيه يعتمد على جلسة المستخدم؛ يجب تسجيل الدخول بحساب التاجر لاختبار لوحة التاجر.

تستجيب API الإنتاج مباشرة: `/api/home/` و`/api/products/` و`/api/vendors/` بحالة 200، بينما `/api/orders/` و`/api/wallets/` و`/api/auth/me/` تعيد 401 بدون Token كما هو متوقع. يوجد في الإنتاج حاليًا تصنيف واحد باسم «ازياء» ولا توجد منتجات منشورة في الاستجابة العامة.

تم إصلاح عقد الطلب في الواجهة ليتوافق مع Django: استخدام `/api/orders/`، وإرسال `items` مع `product_id`، وتطبيع استجابة Django. تم كذلك إصلاح استدعاء Hook السلة داخل checkout، وإضافة شاشة إدارة الفئات، وجعل تمرير لوحة المدير والمنتجات والطلبات وحساب العميل ونموذج منتج التاجر يمتد حتى نهاية المحتوى.

## فحص شاشة الدخول

في `http://127.0.0.1:8083/login` ظهرت شاشة الدخول كاملة في الويب المحلي: الشعار والعنوان والوصف وحقل رقم الهاتف وحقل كلمة المرور وخيار استعادة كلمة المرور وزر الدخول ورابط إنشاء الحساب. لم يظهر شريط تبويب العميل على الشاشة. لم يتم إرسال بيانات دخول أثناء هذا الفحص، لأن المطلوب الحالي اختبار الواجهة دون تنفيذ تسجيل دخول تلقائي.

## فحص دخول التاجر

في `http://127.0.0.1:8082/vendor/login` ظهرت بوابة التاجر كاملة في الويب المحلي: عنوان بوابة الشركاء، عنوان إدارة المتجر، الوصف، حقل رقم الهاتف، حقل كلمة المرور، زر دخول التاجر، ورابط العودة إلى المتجر. لم يظهر شريط العميل السفلي على هذه الشاشة.
