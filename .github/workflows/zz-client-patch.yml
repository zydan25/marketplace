name: zz client patch
'on':
  push:
    branches:
      - feat/api-legacy-integration-cleanup
permissions:
  contents: write
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: verify
        run: echo "client patch workflow active"